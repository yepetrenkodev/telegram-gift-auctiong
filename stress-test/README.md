# 🤖 Stress Test Module

Отдельный модуль для стресс-тестирования аукционной платформы с торговыми ботами.

## Быстрый старт

```bash
cd stress-test
npx ts-node cli.ts
```

Затем в интерактивной консоли:
```
stress-test> start
```

## Опции запуска

```bash
# Базовый тест (10 ботов, 5 аукционов)
npx ts-node cli.ts

# Указать количество ботов и аукционов
npx ts-node cli.ts --bots 20 --auctions 10

# Фиксированная длительность аукционов (секунды)
npx ts-node cli.ts --duration 120

# Диапазон длительности
npx ts-node cli.ts --min-duration 60 --max-duration 300

# Подробный лог
npx ts-node cli.ts --verbose
```

## Интерактивные команды

| Команда | Описание |
|---------|----------|
| `start` | Запустить тест |
| `stop` | Остановить тест |
| `status` | Показать статус |
| `add N` | Добавить N аукционов |
| `bots` | Статистика ботов |
| `config` | Показать конфигурацию |
| `cleanup` | Удалить тестовые данные |
| `exit` | Выйти |

## Предустановленные сценарии

```bash
# Быстрый тест (5 ботов, 3 аукциона, 60с)
npm run quick

# Средний тест (15 ботов, 10 аукционов, 120с)
npm run medium

# Тяжёлый тест (30 ботов, 20 аукционов, 300с)
npm run heavy
```

## Конфигурация

Редактируйте `config.ts` для изменения:

- Количество ботов и их балансы
- Задержки между ставками
- Вероятность ставки
- Параметры аукционов
- URL API

## Удаление модуля

Для удаления стресс-теста просто удалите папку `stress-test/` и уберите строку в `src/routes/index.ts`:

```typescript
// Удалить эту строку:
router.use('/stress-test', stressTestRoutes);
```

## API Endpoints (dev only)

- `POST /api/stress-test/auctions` - Создать тестовый аукцион
- `DELETE /api/stress-test/auctions/:id` - Удалить аукцион
- `DELETE /api/stress-test/auctions` - Удалить все тестовые аукционы
- `POST /api/stress-test/bots` - Создать тестового бота
- `DELETE /api/stress-test/bots` - Удалить всех ботов
- `GET /api/stress-test/stats` - Статистика тестов
